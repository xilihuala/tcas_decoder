架构：
* 1. C 和 S 如何进入主循环（目前主循环仅轮询了S的接收buffer）
* 2. 中断接口需要设定触发方式
3. SendReportToFPGA可优化，源地址可考虑在S和C模式中重用。

S模式：
* 1. 获取差通道方向： 差方向用和方向（状态字中）和GT（数据的最高比特）共同决定。
* 2. 差通道帧头数据：去除GT比特
3. 不同模式的帧格式和状态字定义不同，需要单独处理
* 4. 接收buffer满对应的处理

C模式：
1. *目前的实现会导致出现C2-SPI幻影时正常帧被剔除，而幻影留下
  解决方法： 当出现可能的C2-SPI(即脉冲位置重叠（C2)、和通道相关、差通道相关时，认定后者为SPI，即具有一致性的疑似C2-SPI帧被认定为C2-SPI）。

其它：
1. 双buffer的接收中断用一个函数完成？


额外需要检查的内容：
1. pinmux是否有变化，目前只做了CS2和CS3的pinmux设置


C 模式应将状态传送放入DMA过程中，才能保证数据和长度字段的对应关系

------------------
TODO LIST:
-------------------
代码置信度判定中目前尚未使用二维方法进行判定。二维判定法需要单脉冲值？？？
(目前从其他文献看到的单脉冲值应该是指OBA，需要确定OBA关联的方法（如果OBA不是线性关系，用差值法将不太准确）？？？）
